import React, { useState } from 'react';
import Header from '../components/Header';

const Events = (props) => {
  return (
    <div>
      <Header title={'Events'} />
    </div>
  )
}
export default Events;
