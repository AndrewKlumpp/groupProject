import React, { useState } from 'react';
import Header from '../components/Header';

const Login = (props) => {
  return (
    <div>
      <Header title={'Login'} />
    </div>
  )
}
export default Login;
